services:
  - type: web
    name: eretail-combined
    runtime: node
    rootDir: .
    buildCommand: npm ci --include=dev && npm run build && if [ -n "$MONGODB_URI" ]; then npm run db:init; else echo "MONGODB_URI not set; skipping db:init"; fi
    startCommand: npm run start:all
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: JWT_SECRET
        sync: false
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB_NAME
        value: eretail_replica
      - key: API_PORT
        value: "5050"

  - type: web
    name: eretail-api
    runtime: node
    rootDir: .
    buildCommand: npm ci --include=dev && npm run build --workspace apps/api && if [ -n "$MONGODB_URI" ]; then npm run db:init; else echo "MONGODB_URI not set; skipping db:init"; fi
    startCommand: npm run start:api
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: JWT_SECRET
        sync: false
      - key: MONGODB_URI
        sync: false
      - key: MONGODB_DB_NAME
        value: eretail_replica
      - key: WEB_ORIGIN_HOST
        fromService:
          type: web
          name: eretail-web
          envVarKey: RENDER_EXTERNAL_HOSTNAME

  - type: web
    name: eretail-web
    runtime: node
    rootDir: .
    buildCommand: npm ci --include=dev && npm run build --workspace apps/web
    startCommand: npm run start:web
    envVars:
      - key: NODE_ENV
        value: production
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: NEXT_PUBLIC_API_HOST
        fromService:
          type: web
          name: eretail-api
          envVarKey: RENDER_EXTERNAL_HOSTNAME
